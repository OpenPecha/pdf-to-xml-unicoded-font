# Blog article (draft)

Public write-up: problem (glyph vs Unicode, GSUB ligatures, bad ToUnicode),
solution (`pdf-cmap-fix`, OpenPecha font sourcing), bundled **962-key**
`reverse_db.json` (multi-ZIP build), TI1751/TI1055 metrics, limits
(Type0/Identity-H).

**Review:** Ngawang Trinley, Evan Yerburgh, author.

**Google Doc (placeholder — add URL when the draft exists):**

- _TBD — paste the sharing link here_

**RTF parser article:** tracked separately from this repository.
